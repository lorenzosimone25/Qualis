"""CMS Hospital Compare interactive dashboard.

A Plotly Dash application that reads the harmonised pipeline outputs in
``processed/`` and lets the user explore measures and volumes across
hospitals, states, and the national level over time.

Run with:

    python -m dashboard.app
"""
